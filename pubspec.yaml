name: little_words
description: Projet little words
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0-277.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  latlong2: ^0.8.1
  flutter_map: ^3.1.0
  flutter_map_location_marker:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  sqflite:
  json_annotation:
  shared_preferences:
  flutter_riverpod:
  dio:
  provider:
  geolocator:
  http:
  flutter_map:
  flutter_map_location_marker:


flutter:
  uses-material-design: true

